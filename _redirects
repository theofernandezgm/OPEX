# OPEX — block public access to repo-internal files that ship because
# Cloudflare Pages deploys straight from the repo root (no build step,
# no separate publish directory). These files contain no secrets, but
# they document internal deploy details and a to-do list that should not
# be public. See DEPLOY.md for the full explanation.

/DEPLOY.md            /404.html   404
/README.md            /404.html   404
/.gitignore           /404.html   404
/.gitattributes        /404.html    404

# English pages moved to English URLs (2026-09-23). Permanent redirects so the
# old paths, and anything already linking to them, keep working.
/en/servicios     /en/services      301
/en/metodo        /en/method        301
/en/proceso       /en/process       301
/en/nosotros      /en/about         301
/en/contacto      /en/contact       301
/en/aviso-legal   /en/legal-notice  301
/en/privacidad    /en/privacy       301
